[project]
name = "ColmapCamPath"
version = "0.2.2"
description = "COLMAP -> LichtFeld Studio camera path converter"

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.5.1"
required_features = []
